# Bus test: rust-holes, at S46 (2026-08-26)

Scored by the board owner against the `docs-bustest` checklist, as a
consumer would meet the repo at `b2f57aa`: README.md, CONSUMING.md,
PLAYBOOK.md, EXTRACTION.md, FEEDBACK.md, bin/check, templates/,
examples/, docs/plans/.

Score: 18/24 (survivable)

## Pass (18)

Orient 4/4: one-liner (README paragraph two); quick start
(CONSUMING.md standalone path, five steps); structure (README
read-order table names every file); architecture (README "How this
composes"). Operate 4/4: env setup (CONSUMING prerequisites); build
and test (`bin/check`, the brief's gate commands); config (no knobs
beyond `BOARDKIT_HOME`, documented); deploy (never published, stated).
Decide 2/3: decision log (EXTRACTION.md per-rule dispositions,
docs/plans/); constraints (Known limits, via the PLAYBOOK map to the
skill). Contribute 2/4: dev workflow (CONSUMING family path commit
standard; FEEDBACK's never-edit-from-a-consumer rule); code quality
(`bin/check`). Discoverability 4/6: handoff docs current (the
execution plan log); single roadmap (the phased plan); cross
references resolve (`bin/check` links rule); no duplication (CONSUMING
points at owners). Quality 2/3: no jargon wall that blocks; dated
(docs/plans, sha stamps).

## P1 (blocking)

None.

## P2 (friction)

- rust-holes has no repo-level agent entry file (no AGENTS.md or
  CLAUDE.md), so a cold agent session starts from nothing; a five-line
  entry pointing at CONSUMING.md would close it. Outside S46's scope
  as minted; raised at Gate U.
- The fmt gate is stated two ways: `templates/dispatch-brief.md` has
  the skill's plain form, `templates/skeleton-conventions.md` and
  `templates/golden-frame-harness.md` still say nightly-first. Already
  recorded as the S4 residual on EXTRACTION's fmt-gate row.
- The seeded-defect harness that proves `bin/check` (16 cases) lives
  in the gitignored scratch/ directory; the acceptance runs survive
  only on S45's log. Checking it in beside `bin/check` would make the
  test strategy visible. Outside S45's scope as minted.
- Nothing in README points at docs/plans/, so current status is
  findable only by listing the directory.

## P3 (polish)

- No changelog and no CI; a pre-commit hook running `bin/check` is the
  natural fit for a private docs repo.
- CONSUMING's Ask Mike list says "its card S39" and "S40"; a reader
  with no boardkit context meets the word "card" cold there.

## Agent discoverability

- CLAUDE.md: missing (see P2).
- Handoff docs: current (docs/plans/2026-08-25-cleanup-execution.md
  Log).
- Competing roadmaps: no.
- Duplication: CONSUMING restates the never-publish boundary in one
  sentence and points at EXTRACTION for the rest; accepted.

## Missing docs by quadrant

- Tutorial: missing by design; chore-lottery is the live worked run.
- How-to: CONSUMING.md (consume), FEEDBACK.md (report friction).
- Reference: templates/ and the README table.
- Explanation: EXTRACTION.md and the skill.

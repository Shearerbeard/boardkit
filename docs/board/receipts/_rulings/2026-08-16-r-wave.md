---
receipt: v1
kind: ruling
cards: [S13, S16, S18, S19, S20, S21, S22, S23, S24, S25]
gate: A
verdict: RULING
dated: 2026-08-16
route: codex-reviewer
author_models: [claude-fable-5]
reviewer_models: [gpt-5.6-sol]
rounds:
  - round: 1
    object: "the ten card diffs, one packet each"
    verdict: FAIL
    findings: 24
  - round: 2
    object: "six fix commits"
    verdict: MIXED
    findings: 5
  - round: 3
    object: "2121d41"
    verdict: FAIL
    findings: 3
  - round: 4
    object: "8487140"
    verdict: FAIL
    findings: 3
  - round: 5
    object: "3a4b001"
    verdict: FAIL
    findings: 1
ruling: docs/board/evidence/2026-08-16-gate-a-review-cycle.md
gate_ticked: false
packets: []
---

# Ruling receipt: the 2026-08-16 R-wave Gate A cycle

A backfill receipt, written at backfill time (2026-08-24, under S33) for
the event of 2026-08-16, per ADR 0001 section 4's backfill exception; the
`dated` field carries the event's date. Mike ruled the backfill at the
2026-08-22 Gate U: start fresh, plus this one receipt for the ruling.

The cycle ran five rounds through the codex fallback after the pinned
opencode lane stalled its read probes; the full ledger, the ruling's five
points, and where the per-card material lives are in the attested
document below. Gate A did not tick on any of the ten cards
(`gate_ticked: false`, ruling point 4); the pass decision went to the
user at each card's U(code-review) gate, and Mike accepted the wave on
this record at the 2026-08-22 wave-2 Gate U.

`packets: []` is the honest state: the per-card packets and reviewer
verbatims were never archived anywhere but one machine's gitignored
`docs/board/reviews/`, so there is nothing to point at.

## Digests

| SHA-256 | Path |
| --- | --- |
| 1b9524794a565a716f6912efe90b135f83f06720d834f58b02a0a205a5de8016 | `docs/board/evidence/2026-08-16-gate-a-review-cycle.md` |

## Findings

The per-round findings ledger lives in the attested ruling document and
in each card's own log (round 1's 24 findings were dispositioned per
card there). Every finding against each card's reviewed diff resolved by
round 5; rounds 3-5 each returned one further, narrower evasion of the
S24 fix code's `_is_shim` heuristic, and that residual hardening was
carded as S29 rather than patched a sixth time in-cycle (ruling point 3).

## Checks the reviewer did not run

The reviewer ran under `codex exec --sandbox read-only`, which has no
writable temporary directory or uv cache, so `pytest` and the
`uv`-prefixed board commands were UNVERIFIED by the reviewer at every
round. They were run board-side instead: the suite went 337 to 361 green
across the cycle, with `ruff check` and `boardkit check` clean at each
commit.
